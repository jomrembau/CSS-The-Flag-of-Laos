# CSS-The-Flag-of-Laos
This HTML and CSS project creates the flag of Laos. The flag is 900x600px with red stripes on top and bottom and a blue middle stripe. A white circle (200x200px) is centered in the blue stripe. The text "The Flag" and "of Laos" are centered above the flag and inside the circle, respectively.
